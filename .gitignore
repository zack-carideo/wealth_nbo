# ---------------------------------------------------------------------
# Pipeline outputs. These are regenerated by preprocess.py / model.py and
# are never fixtures — the copies currently in the working tree came from
# a synthetic run. Do not commit them: real runs contain customer data.
# ---------------------------------------------------------------------

# default output directories from the `io:` blocks in the configs
model_data/
model_output/
artifacts/

# preprocess.py
flat_dataset.csv
hazard_dataset.csv
exclusions.csv
percentile_grids.json

# model.py
metrics.csv
metrics_by_repeat.csv
calibration.csv
features.csv
predictions.csv

# make_synthetic.py (default --out, and the path config.yaml ships pointing at)
synthetic.csv
sample_raw_acquisitions.csv

# ---------------------------------------------------------------------
# Python
# ---------------------------------------------------------------------
__pycache__/
*.py[cod]
.venv/
venv/
env/
.ipynb_checkpoints/
